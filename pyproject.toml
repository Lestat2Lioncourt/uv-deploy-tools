[project]
name = "uv-deploy-tools"
version = "0.1.0"
description = "Gestion du d̩ploiement des packages Python dans ORBIT"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "click>=8.3.1",
    "paramiko>=4.0.0",
    "pyyaml>=6.0.3",
    "rich>=14.2.0",
    "tomli>=2.3.0",
]

